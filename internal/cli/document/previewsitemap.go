// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: 495663a8e502

package document

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/voiceflow/cli/internal/client"
	"github.com/voiceflow/cli/internal/flagutil"
	"github.com/voiceflow/cli/internal/interactive"
	"github.com/voiceflow/cli/internal/output"
	"github.com/voiceflow/cli/internal/sdk"
	"github.com/voiceflow/cli/internal/sdk/models/operations"
	"github.com/voiceflow/cli/internal/usage"
)

var previewSitemapCmdMeta = []flagutil.FlagMeta{
	{FlagName: "project-id", Shorthand: "p", FieldPath: "ProjectID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "sitemap-url", Shorthand: "s", FieldPath: "Body.SitemapURL", Kind: flagutil.FlagKindString, Required: true, Description: "The HTTPS URL of the sitemap to read. A bare site URL works too — when it does not end in `.xml`, `/sitemap.xml`, `/sitemap_index.xml` and `/sitemap/sitemap.xml` are probed in turn. [required]"},
	{FlagName: "exclude", Shorthand: "e", FieldPath: "Body.Exclude", Kind: flagutil.FlagKindStringArray, Optional: true, Description: "Patterns that drop a URL from the import when any one of them matches. Matching is case-insensitive and unanchored: a pattern with no `*` matches any URL containing it as a substring (`/blog/`), while `*` matches any run of characters (`*/tag/*`, `*.pdf`)."},
}

// initPreviewSitemapCmd initializes the preview-sitemap command.
func initPreviewSitemapCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "preview-sitemap",
		Short:   "Preview sitemap URLs",
		Long:    "Read a sitemap and return the URLs an import would create documents for.",
		Example: "  vf document preview-sitemap --project-id <id> --sitemap-url https://fake-marimba.org",
		RunE:    runPreviewSitemapCmd,
		Aliases: []string{"ps"},
	}
	flagutil.RegisterFlags(cmd, previewSitemapCmdMeta)
	if err := flagutil.ValidateMeta[operations.StableDocumentControllerPreviewSitemapRequest](previewSitemapCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for preview-sitemap: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runPreviewSitemapCmd executes the preview-sitemap command.
func runPreviewSitemapCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, previewSitemapCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, previewSitemapCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.StableDocumentControllerPreviewSitemapRequest](cmd, previewSitemapCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Document.PreviewSitemap(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
