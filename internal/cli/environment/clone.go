// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: b1eca597c998

package environment

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/voiceflow/cli/internal/client"
	"github.com/voiceflow/cli/internal/flagutil"
	"github.com/voiceflow/cli/internal/interactive"
	"github.com/voiceflow/cli/internal/output"
	"github.com/voiceflow/cli/internal/sdk"
	"github.com/voiceflow/cli/internal/sdk/models/operations"
	"github.com/voiceflow/cli/internal/usage"
)

var cloneCmdMeta = []flagutil.FlagMeta{
	{FlagName: "environment-alias", Shorthand: "e", FieldPath: "EnvironmentAlias", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "project-id", Shorthand: "p", FieldPath: "ProjectID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Required: true, Description: "The display name of the new environment. [required]"},
	{FlagName: "alias", Shorthand: "a", FieldPath: "Body.Alias", Kind: flagutil.FlagKindString, Required: true, Description: "Short URL-safe identifier; cannot be 'main' (reserved). [required]"},
}

// initCloneCmd initializes the clone command.
func initCloneCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "clone",
		Short:   "Clone environment",
		Long:    "Clone an environment.",
		Example: "  vf environment clone --environment-alias <value> --project-id <id> --name <value> --alias <value>",
		RunE:    runCloneCmd,
	}
	flagutil.RegisterFlags(cmd, cloneCmdMeta)
	if err := flagutil.ValidateMeta[operations.StableEnvironmentControllerCloneRequest](cloneCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for clone: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runCloneCmd executes the clone command.
func runCloneCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, cloneCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, cloneCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.StableEnvironmentControllerCloneRequest](cmd, cloneCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Environment.Clone(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
