// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: e926eb890409

package environment

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/voiceflow/cli/internal/client"
	"github.com/voiceflow/cli/internal/flagutil"
	"github.com/voiceflow/cli/internal/interactive"
	"github.com/voiceflow/cli/internal/output"
	"github.com/voiceflow/cli/internal/sdk"
	"github.com/voiceflow/cli/internal/sdk/models/operations"
	"github.com/voiceflow/cli/internal/usage"
)

var publishCmdMeta = []flagutil.FlagMeta{
	{FlagName: "environment-alias", Shorthand: "e", FieldPath: "EnvironmentAlias", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "project-id", Shorthand: "p", FieldPath: "ProjectID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Required: true, Description: "The name of the release created by publishing. [required]"},
	{FlagName: "description", FieldPath: "Body.Description", Kind: flagutil.FlagKindJSON, Optional: true, HasDefault: true, DefaultStr: "null", Annotations: `default:"null" json:"description"`, Description: "An optional description of what changed in this release."},
}

// initPublishCmd initializes the publish command.
func initPublishCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "publish",
		Short:   "Publish environment",
		Long:    "Publish live version of an environment.",
		Example: "  vf environment publish --environment-alias <value> --project-id <id> --name <value>",
		RunE:    runPublishCmd,
	}
	flagutil.RegisterFlags(cmd, publishCmdMeta)
	if err := flagutil.ValidateMeta[operations.StableEnvironmentControllerPublishRequest](publishCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for publish: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runPublishCmd executes the publish command.
func runPublishCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, publishCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, publishCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.StableEnvironmentControllerPublishRequest](cmd, publishCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Environment.Publish(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
