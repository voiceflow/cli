// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
// @generated-id: f39d780b2574

package run

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/voiceflow/cli/internal/client"
	"github.com/voiceflow/cli/internal/flagutil"
	"github.com/voiceflow/cli/internal/interactive"
	"github.com/voiceflow/cli/internal/output"
	"github.com/voiceflow/cli/internal/sdk"
	"github.com/voiceflow/cli/internal/sdk/models/operations"
	"github.com/voiceflow/cli/internal/usage"
)

var searchCmdMeta = []flagutil.FlagMeta{
	{FlagName: "project-id", Shorthand: "p", FieldPath: "ProjectID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "environment-alias", Shorthand: "e", FieldPath: "EnvironmentAlias", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "take", FieldPath: "Body.Take", Kind: flagutil.FlagKindFloat64, Optional: true, HasDefault: true, DefaultFloat: 20, Description: "The maximum number of results to return, used for pagination."},
	{FlagName: "skip", Shorthand: "s", FieldPath: "Body.Skip", Kind: flagutil.FlagKindInt64, Optional: true, HasDefault: true, Description: "The number of results to skip, used for pagination."},
	{FlagName: "test-id", FieldPath: "Body.TestID", Kind: flagutil.FlagKindString, Optional: true, Description: "Restrict results to runs of this test. Omit to search across all tests."},
}

// initSearchCmd initializes the search command.
func initSearchCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "search",
		Short:   "Search runs",
		Long:    "Search runs by project ID.",
		Example: "  vf run search --project-id <id> --environment-alias <value>",
		RunE:    runSearchCmd,
	}
	flagutil.RegisterFlags(cmd, searchCmdMeta)
	if err := flagutil.ValidateMeta[operations.StableTestRunControllerSearchRequest](searchCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for search: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runSearchCmd executes the search command.
func runSearchCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, searchCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, searchCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.StableTestRunControllerSearchRequest](cmd, searchCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if err := output.ValidateGlobalServerIndex(cmd, len(sdk.ServerList)); err != nil {
		return err
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Test.Run.Search(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
