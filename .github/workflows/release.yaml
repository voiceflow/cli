name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    # prerelease.yaml pushes tags like v0.255.0-pr.42.7 for PR builds, which
    # match the v* trigger above. Pushes authenticated with GITHUB_TOKEN do not
    # start workflow runs, so this should never fire — but a real npm publish
    # is not something to leave resting on one undocumented platform behaviour.
    if: ${{ !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"
          cache: true
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: release-artifacts
          path: |
            dist/
            !dist/*.txt
          retention-days: 30

  npm-publish:
    needs: goreleaser
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # npm --provenance (Sigstore via GitHub OIDC)
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - name: Download goreleaser dist
        uses: actions/download-artifact@v4
        with:
          name: release-artifacts
          path: dist

      - name: Assemble npm packages
        run: node npm/scripts/prepare.mjs --version "${GITHUB_REF_NAME#v}" --dist dist --out dist/npm

      - name: Smoke test staged linux-x64 binary
        run: dist/npm/cli-linux-x64/bin/vf version

      - name: Publish (idempotent)
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: node npm/scripts/publish.mjs --version "${GITHUB_REF_NAME#v}" --out dist/npm
